#pragma once

#include <vector>

using std::list;
using std::deque;

void generate_random_grades( std::vector<unsigned> &, unsigned, bool );
std::vector<unsigned> generate_random_grades( unsigned );
