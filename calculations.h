#pragma once

#include "student.h"
#include <vector>

using std::list;
using std::deque;
using std::search;
using std::copy;
using std::remove_copy;

void calculate_med_avg( Student &, const std::deque<unsigned> & );
